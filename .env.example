# Browserbase credentials
# Get these from https://www.browserbase.com/dashboard
BROWSERBASE_API_KEY=your_browserbase_api_key_here
BROWSERBASE_PROJECT_ID=your_browserbase_project_id_here

# Gemini / model configuration for Stagehand
# Default model is Gemini 2.5 Flash; you can override to a Gemini 3 Flash model string when supported.
# Stagehand will read the Gemini API key from GOOGLE_GENERATIVE_AI_API_KEY.
GOOGLE_GENERATIVE_AI_API_KEY=your_gemini_api_key_here
STAGEHAND_MODEL=google/gemini-2.5-flash

# Database configuration (for Railway or local Postgres)
# The scraper will create the posts table automatically on first run
DATABASE_URL=postgresql://user:password@host:5432/paul_chek_blog

# Scraper configuration
# Controls how many pagination pages to visit per Doctor section
# Full scrape requires ~35 pages per section; set to 100 to get everything
# Conservative default (2) for cheap test runs
MAX_PAGES_PER_SECTION=2
